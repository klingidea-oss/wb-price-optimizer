# 🔌 Руководство по интеграции с вашим API Wildberries

## Что нужно для работы приложения

### 1. API ключ Wildberries (обязательно)

Получите API ключ в личном кабинете Wildberries:
1. Зайдите в личный кабинет продавца
2. Перейдите в раздел "Настройки" → "Доступ к API"
3. Создайте новый ключ с правами:
   - ✅ Чтение статистики
   - ✅ Управление ценами
   - ✅ Чтение информации о товарах

### 2. Эндпоинты API, которые использует приложение

#### 2.1 Получение статистики продаж
```
GET https://suppliers-api.wildberries.ru/content/v1/analytics/nm-report/detail
```

**Параметры:**
- `nmID` - артикул товара
- `dateFrom` - начальная дата (YYYY-MM-DD)
- `dateTo` - конечная дата (YYYY-MM-DD)

**Ответ должен содержать:**
```json
{
  "data": [
    {
      "date": "2025-12-01",
      "finishedPrice": 150000,  // ВАЖНО! Цена со скидкой в копейках
      "price": 200000,           // Цена без скидки в копейках
      "quantity": 15,            // Количество продаж
      "retail_amount": 2250000   // Выручка в копейках
    }
  ]
}
```

#### 2.2 Обновление цен
```
POST https://suppliers-api.wildberries.ru/public/api/v1/prices
```

**Тело запроса:**
```json
[
  {
    "nmId": 123456789,
    "price": 150000  // Цена со скидкой в копейках
  }
]
```

#### 2.3 Информация о товарах
```
POST https://suppliers-api.wildberries.ru/content/v1/cards/filter
```

**Тело запроса:**
```json
{
  "vendorCodes": ["123456789"]
}
```

#### 2.4 Публичный API для карточек товаров
```
GET https://card.wb.ru/cards/v1/detail?appType=1&curr=rub&dest=-1257786&spp=30&nm={nm_id}
```

Этот эндпоинт используется для получения информации о конкурентах.

**Ответ:**
```json
{
  "data": {
    "products": [
      {
        "id": 123456789,
        "name": "Название товара",
        "brand": "Бренд",
        "salePriceU": 150000,  // Цена со скидкой в копейках
        "priceU": 200000,      // Цена без скидки в копейках
        "rating": 4.8,
        "feedbacks": 1250,     // Количество отзывов
        "subjectName": "Категория",
        "supplierId": 12345
      }
    ]
  }
}
```

#### 2.5 Поиск товаров
```
GET https://search.wb.ru/exactmatch/ru/common/v4/search
```

**Параметры:**
- `query` - поисковый запрос (категория)
- `resultset` - "catalog"
- `sort` - "popular"

Используется для поиска конкурентов в категории.

## Структура данных, которую ожидает приложение

### Добавление товара в систему

```python
# POST /products
{
    "nm_id": 123456789,           # Артикул WB
    "name": "Название товара",     
    "category": "Категория",       # Опционально
    "current_price": 1500,         # ЦЕНА СО СКИДКОЙ!
    "cost_price": 800              # Ваша себестоимость
}
```

### Важные поля для анализа конкурентов

Для правильной работы анализа конкурентов нужны:

1. **Цена со скидкой** (`finishedPrice` / `salePriceU`)
2. **Категория** (`subjectName`)
3. **Размер** (если применимо)
4. **Количество отзывов** (`feedbacks`)
5. **ID поставщика** (`supplierId`) - чтобы исключить свои товары

## Адаптация под ваш API

### Если у вас другая структура ответа

#### Шаг 1: Откройте `wb_api_client.py`

Найдите метод `get_sales_history()`:

```python
async def get_sales_history(self, nm_id: int, days: int = 30) -> List[Dict]:
    # ...
    
    if "data" in stats:
        for record in stats["data"]:
            # ВАШ КОД: измените маппинг полей
            price_with_discount = record.get("finishedPrice", 0) / 100
            original_price = record.get("price", 0) / 100
            
            sales_history.append({
                "date": datetime.fromisoformat(record.get("date", "")),
                "price_with_discount": price_with_discount,
                "original_price": original_price,
                "sales_count": record.get("quantity", 0),
                "revenue": record.get("retail_amount", 0) / 100
            })
```

**Адаптируйте под свои поля:**
- Замените `"finishedPrice"` на ваше поле цены со скидкой
- Замените `"quantity"` на ваше поле количества продаж
- Замените `"retail_amount"` на ваше поле выручки

#### Шаг 2: Откройте `competitor_analyzer.py`

Найдите метод `_get_product_details()`:

```python
# Извлечение данных
salePriceU = product.get("salePriceU", 0) / 100  # Цена со скидкой
priceU = product.get("priceU", 0) / 100          # Цена без скидки

return {
    "nm_id": nm_id,
    "name": product.get("name", ""),
    "brand": product.get("brand", ""),
    "category": product.get("subjectName", ""),
    "price_with_discount": salePriceU,
    "original_price": priceU,
    # ... остальные поля
}
```

**Адаптируйте под структуру вашего API.**

### Если у вас другие эндпоинты

Откройте `config.py` и измените URL:

```python
class Config:
    # Измените на ваши эндпоинты
    WB_API_BASE_URL = "https://your-api.wildberries.ru"
    WB_STATISTICS_URL = f"{WB_API_BASE_URL}/your/stats/endpoint"
    WB_PRICES_URL = f"{WB_API_BASE_URL}/your/prices/endpoint"
```

## Проверка интеграции

### Тест 1: Получение статистики

```python
import asyncio
from wb_api_client import WildberriesAPIClient

async def test_stats():
    client = WildberriesAPIClient("YOUR_API_KEY")
    stats = await client.get_sales_history(123456789, days=7)
    print(f"Получено точек данных: {len(stats)}")
    if stats:
        print(f"Первая запись: {stats[0]}")

asyncio.run(test_stats())
```

### Тест 2: Получение информации о товаре

```python
async def test_product_info():
    client = WildberriesAPIClient("YOUR_API_KEY")
    info = await client.get_product_card_details(123456789)
    print(f"Цена со скидкой: {info.get('price_with_discount')}")
    print(f"Отзывов: {info.get('reviews_count')}")

asyncio.run(test_product_info())
```

### Тест 3: Поиск конкурентов

```python
from competitor_analyzer import CompetitorAnalyzer

async def test_competitors():
    analyzer = CompetitorAnalyzer("YOUR_API_KEY")
    # Нужен существующий товар в БД
    result = await analyzer.analyze_competitors(
        session=None,  # Передайте реальную сессию
        nm_id=123456789,
        min_reviews=500
    )
    print(f"Найдено конкурентов: {result['total_competitors']}")

# asyncio.run(test_competitors())
```

## Особенности работы с ценами со скидкой

### Почему используются цены со скидкой?

1. **Реальность:** Покупатели видят и реагируют на цены со скидкой
2. **Эластичность:** Спрос зависит от финальной цены, а не базовой
3. **Конкуренция:** Конкуренты сравниваются по финальным ценам
4. **Точность:** Прогнозы более точные при использовании реальных цен

### Что делать, если у вас нет скидок?

Если `finishedPrice` равен `price`, всё работает корректно:

```python
# В wb_api_client.py
price_with_discount = record.get("finishedPrice", 0) / 100

# Если finishedPrice нет, используем price
if price_with_discount == 0:
    price_with_discount = record.get("price", 0) / 100
```

## Часто задаваемые вопросы

### Q: Нужно ли менять основную логику?
**A:** Нет. Просто адаптируйте маппинг полей в `wb_api_client.py` и `competitor_analyzer.py`.

### Q: Что если структура JSON отличается?
**A:** Измените методы парсинга в соответствующих классах. Ключевые места помечены комментариями `# Парсинг ответа зависит от структуры API WB`.

### Q: Можно ли использовать другую БД вместо SQLite?
**A:** Да. Измените `DATABASE_URL` в `.env`:
```
DATABASE_URL=postgresql+asyncpg://user:pass@host/db
```

### Q: Как обрабатываются товары без истории продаж?
**A:** Система требует минимум 7 точек данных. Если данных меньше, вернется ошибка с сообщением.

### Q: Можно ли отключить анализ конкурентов?
**A:** Да. Установите `consider_competitors=false` в запросе:
```python
POST /optimize/{nm_id}?consider_competitors=false
```

### Q: Что если API конкурентов недоступен?
**A:** Оптимизация продолжится без учета конкурентов, используя только анализ эластичности.

## Поддержка

Если возникли проблемы с интеграцией:

1. Проверьте логи приложения
2. Убедитесь, что API ключ валиден
3. Проверьте структуру ответов вашего API
4. Сравните с примерами в документации WB

## Следующие шаги

После успешной интеграции:

1. ✅ Добавьте несколько тестовых товаров
2. ✅ Проверьте анализ конкурентов
3. ✅ Запустите оптимизацию
4. ✅ Изучите рекомендации AI
5. ✅ Примените цены в тестовом режиме
6. ✅ Мониторьте результаты

Удачной оптимизации! 🚀
