# 🚂 Развертывание на Railway.app (Постоянный домен бесплатно!)

## Почему Railway?

✅ **Бесплатно навсегда**  
✅ **Постоянный домен** без случайных символов  
✅ **Автоматический HTTPS**  
✅ **Работает 24/7**  
✅ **Автообновление** из GitHub  
✅ **База данных** SQLite сохраняется  

Ваш домен будет: `https://wb-optimizer.railway.app` (настраивается)

## 📋 Шаг 1: Подготовка файлов (уже готовы!)

Я создал все необходимые файлы:

### `railway.json` - конфигурация Railway
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### `runtime.txt` - версия Python
```
python-3.11
```

### `Procfile` - команда запуска
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

## 📋 Шаг 2: Зарегистрируйтесь на Railway (1 минута)

1. Перейдите на https://railway.app/
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub** (рекомендуется)

## 📋 Шаг 3: Создайте GitHub репозиторий (2 минуты)

### Вариант A: Через GitHub Desktop (проще)

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Откройте папку `wb_price_optimizer`
3. **File** → **Add Local Repository**
4. Выберите папку
5. **Publish repository** → Назовите `wb-price-optimizer`
6. Нажмите **Publish**

### Вариант B: Через командную строку

```bash
cd wb_price_optimizer

# Инициализируйте git (если ещё не сделано)
git init

# Добавьте файлы
git add .

# Сделайте коммит
git commit -m "Initial commit"

# Создайте репозиторий на GitHub (нужен GitHub CLI)
gh repo create wb-price-optimizer --public --source=. --push

# Или создайте вручную на github.com и запушьте:
git remote add origin https://github.com/ваш-username/wb-price-optimizer.git
git branch -M main
git push -u origin main
```

## 📋 Шаг 4: Разверните на Railway (1 минута!)

1. Откройте https://railway.app/new
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `wb-price-optimizer`
4. Railway автоматически определит Python приложение
5. Нажмите **"Deploy"**

⏳ Подождите 2-3 минуты - Railway соберет приложение

## 📋 Шаг 5: Настройте переменные окружения

В Railway Dashboard:

1. Перейдите в ваш проект
2. Откройте вкладку **"Variables"**
3. Добавьте:

```
WB_API_KEY=ваш_api_ключ_wildberries
OPENAI_API_KEY=ваш_openai_ключ (опционально)
```

4. Нажмите **"Save"**

Railway автоматически перезапустит приложение.

## 📋 Шаг 6: Настройте кастомный домен (опционально)

### Вариант A: Railway субдомен (бесплатно)

1. Откройте **"Settings"**
2. Найдите **"Domains"**
3. Нажмите **"Generate Domain"**
4. Получите: `https://wb-optimizer.up.railway.app`

### Вариант B: Ваш собственный домен

1. В **"Domains"** нажмите **"Custom Domain"**
2. Введите ваш домен: `dashboard.yourcompany.com`
3. Добавьте CNAME запись в DNS:
   ```
   CNAME: dashboard → your-app.up.railway.app
   ```
4. Подождите 5-10 минут

Railway автоматически настроит HTTPS!

## 🎉 Готово!

Ваш дашборд теперь доступен 24/7:
```
https://wb-optimizer.up.railway.app
```

## 📊 Мониторинг

Railway предоставляет:
- 📈 **Графики использования** CPU/RAM
- 📝 **Логи в реальном времени**
- 🔄 **История деплоев**
- 💰 **Использование бесплатных часов**

Доступно в Railway Dashboard.

## 🔄 Автообновление

Каждый раз, когда вы пушите в GitHub:
```bash
git add .
git commit -m "Обновление"
git push
```

Railway **автоматически** обновит приложение! 🚀

## 💾 База данных

SQLite база сохраняется между деплоями в Railway Volume.

Для более надежного хранения можно использовать:
- Railway PostgreSQL (бесплатно)
- Railway MySQL (бесплатно)

## 🔒 Безопасность

### Добавьте базовую аутентификацию

Создайте файл `middleware.py`:
```python
from fastapi import Request, HTTPException, status
from fastapi.security import HTTPBasic, HTTPBasicCredentials
import secrets

security = HTTPBasic()

def verify_credentials(credentials: HTTPBasicCredentials):
    correct_username = secrets.compare_digest(credentials.username, "admin")
    correct_password = secrets.compare_digest(credentials.password, "your_password")
    if not (correct_username and correct_password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Incorrect username or password",
            headers={"WWW-Authenticate": "Basic"},
        )
    return credentials.username
```

Добавьте в `main.py`:
```python
from middleware import security, verify_credentials
from fastapi import Depends

# Защитите все эндпоинты
@app.get("/", dependencies=[Depends(verify_credentials)])
async def root():
    ...
```

### Используйте переменные окружения для паролей

В Railway Variables:
```
ADMIN_USERNAME=ваш_логин
ADMIN_PASSWORD=ваш_пароль
```

## ⚠️ Лимиты бесплатного тарифа

Railway Free Plan:
- ✅ 500 часов/месяц выполнения
- ✅ 1 GB RAM
- ✅ Неограниченный трафик
- ✅ Автоматические деплои
- ❌ Спит после 30 мин неактивности

**Решение:** Railway Pro ($5/мес) - без ограничений

## 🚀 Альтернативы Railway

Если нужно больше бесплатных часов:

### Render.com
```bash
# Инструкции в DEPLOY_RENDER.md
```

### Fly.io
```bash
# Инструкции в DEPLOY_FLY.md
```

### Vercel (только для фронтенда)
- Не подходит для FastAPI с базой данных

## 🐛 Решение проблем

### Ошибка: Application failed to start

**Проблема:** Неправильная команда запуска

**Решение:**
1. Проверьте `Procfile`:
   ```
   web: uvicorn main:app --host 0.0.0.0 --port $PORT
   ```
2. Или в Railway Settings → Deploy → Start Command:
   ```
   uvicorn main:app --host 0.0.0.0 --port $PORT
   ```

### Ошибка: Module not found

**Проблема:** Зависимости не установлены

**Решение:**
1. Проверьте `requirements.txt` в корне
2. В Railway логах должно быть: `Installing dependencies from requirements.txt`

### База данных пустая после деплоя

**Проблема:** SQLite не сохраняется

**Решение:**
1. Railway → Settings → Volumes
2. Создайте Volume: `/data`
3. Измените `config.py`:
   ```python
   DATABASE_URL = "sqlite+aiosqlite:////data/wb_optimizer.db"
   ```

## 📱 Мобильное приложение

Ваш дашборд теперь доступен на мобильных!

Добавьте PWA (Progressive Web App):
1. Создайте `manifest.json`
2. Добавьте Service Worker
3. Пользователи смогут "установить" дашборд как приложение

(Инструкции в `PWA_SETUP.md` если нужно)

## 🎯 Best Practices

1. **Используйте production settings:**
   ```python
   # config.py
   DEBUG = False
   LOG_LEVEL = "INFO"
   ```

2. **Настройте мониторинг:**
   - Railway Analytics
   - Sentry для ошибок
   - Логи в облако

3. **Регулярные бэкапы БД:**
   ```bash
   # Скачивайте базу периодически
   railway run python backup_db.py
   ```

4. **Версионирование:**
   ```bash
   git tag v1.0.0
   git push --tags
   ```

## 🎉 Поздравляю!

Ваш дашборд теперь доступен всему миру 24/7!

**Поделитесь ссылкой:**
```
https://wb-optimizer.up.railway.app
```

---

💡 **Совет:** Добавьте собственный домен для профессионального вида!
