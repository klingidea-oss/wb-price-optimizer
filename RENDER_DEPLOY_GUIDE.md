# 🚀 Деплой WB Price Optimizer на Render.com

## ✅ Полностью бесплатный деплой без кредитной карты

---

## 📋 Что вам понадобится

1. ✅ Аккаунт GitHub (уже создан)
2. ✅ Git установлен на компьютере (устанавливается)
3. ✅ Файлы приложения (уже готовы)
4. 🆕 Аккаунт Render.com (создадим за 2 минуты)

---

## 🎯 Шаг 1: Загрузка кода на GitHub

### 1.1 Откройте терминал / командную строку

**Windows:**
- Нажмите `Win + R`, введите `cmd`, нажмите Enter
- Или: PowerShell

**macOS/Linux:**
- Откройте Terminal

### 1.2 Перейдите в папку с приложением

```bash
# Замените путь на реальный путь к вашей папке wb_price_optimizer
cd /путь/к/папке/wb_price_optimizer

# Пример для Windows:
# cd C:\Users\YourName\Desktop\wb_price_optimizer

# Пример для macOS:
# cd ~/Desktop/wb_price_optimizer
```

### 1.3 Выполните команды Git

**⚠️ ВАЖНО: Замените `YOUR_GITHUB_USERNAME` на ваш настоящий логин GitHub!**

```bash
# Инициализация Git репозитория
git init

# Настройка Git (если делаете первый раз)
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"

# Добавление всех файлов
git add .

# Создание коммита
git commit -m "Initial deployment for Render"

# Переименование ветки в main
git branch -M main

# Привязка к GitHub репозиторию (ЗАМЕНИТЕ YOUR_GITHUB_USERNAME!)
git remote add origin https://github.com/YOUR_GITHUB_USERNAME/wb-price-optimizer.git

# Отправка кода на GitHub
git push -u origin main
```

**При запросе логина/пароля GitHub:**
- Логин: ваш GitHub username
- Пароль: **Personal Access Token** (не пароль от аккаунта!)

### Как получить Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте название: `wb-optimizer-deploy`
4. Выберите срок: `90 days`
5. Поставьте галочку на **`repo`** (все подпункты)
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (он покажется только один раз!)
8. Используйте этот токен вместо пароля при `git push`

---

## 🎯 Шаг 2: Регистрация на Render.com

1. Откройте: **https://render.com/register**
2. Нажмите **"Sign up with GitHub"**
3. Разрешите доступ Render к GitHub
4. Подтвердите email (если попросят)

✅ **Готово!** Вы на Dashboard Render.

---

## 🎯 Шаг 3: Создание Web Service

### 3.1 Создайте новый сервис

1. На Dashboard нажмите **"+ New"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Нажмите **"Next"**

### 3.2 Подключите репозиторий

1. Найдите в списке **`wb-price-optimizer`**
2. Нажмите **"Connect"**

**Если репозиторий не виден:**
- Нажмите **"Configure account"**
- Дайте Render доступ к нужным репозиториям
- Обновите страницу

### 3.3 Настройте сервис

**Заполните форму:**

| Параметр | Значение |
|----------|----------|
| **Name** | `wb-price-optimizer` |
| **Region** | `Frankfurt (EU Central)` (ближе к России) |
| **Branch** | `main` |
| **Root Directory** | (оставьте пустым) |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python main.py` |

**Instance Type:** Выберите **`Free`** ✅

**Прокрутите вниз и нажмите:** **"Create Web Service"**

---

## 🎯 Шаг 4: Добавление переменных окружения

Пока идёт первый деплой (2-3 минуты), добавим API ключи:

1. В левом меню нажмите **"Environment"**
2. Добавьте переменные:

### Обязательная переменная:

**Key:** `WB_API_KEY`  
**Value:** 
```
eyJhbGciOiJFUzI1NiIsImtpZCI6IjIwMjUwOTA0djEiLCJ0eXAiOiJKV1QifQ..sXVhc06l1xxfFV0YPh7mw0P3x2splzZVtZBRB0SjZLmo_DL2ebZqTfNGrzOuVGDlk5V_ndFeynZs_244eiuB2A
```

### Опциональная переменная (для AI рекомендаций):

**Key:** `OPENAI_API_KEY`  
**Value:** `ваш_ключ_openai` (если есть)

3. Нажмите **"Save Changes"**

Приложение автоматически перезапустится с новыми переменными.

---

## 🎯 Шаг 5: Получение публичного URL

### 5.1 Дождитесь завершения деплоя

В верхней части страницы вы увидите статус:
- 🟡 **"In Progress"** → Деплой идёт
- 🟢 **"Live"** → Приложение запущено!

### 5.2 Найдите ваш URL

В верхней части страницы будет ссылка типа:
```
https://wb-price-optimizer.onrender.com
```

**Это ваш постоянный домен!** Он не изменится.

---

## 🎉 Готово! Проверка работы

### Откройте дашборд:
```
https://wb-price-optimizer.onrender.com
```

### Откройте API документацию:
```
https://wb-price-optimizer.onrender.com/docs
```

---

## ⚠️ Важные особенности бесплатного плана Render

### 1. "Засыпание" сервиса

**Проблема:** После 15 минут неактивности сервис "засыпает".

**Решение:** При первом обращении он "проснётся" за ~30 секунд.

**Как предотвратить:**
```bash
# Вариант 1: Cron-сервис (пингует каждые 10 минут)
# Добавьте в Render отдельный Cron Job:

curl https://wb-price-optimizer.onrender.com/health
```

### 2. Ограничения

- ✅ 750 часов работы в месяц (≈30 дней)
- ✅ Автоматический SSL (HTTPS)
- ✅ Неограниченное количество запросов
- ⚠️ Медленнее при "пробуждении" (~30 сек)
- ⚠️ Лимит памяти: 512 MB

---

## 🔄 Обновление приложения

При любых изменениях в коде:

```bash
cd /путь/к/папке/wb_price_optimizer

git add .
git commit -m "Описание изменений"
git push
```

Render автоматически пересоберёт и задеплоит новую версию!

---

## 🆙 Апгрейд до платного плана (опционально)

Если понадобится, можете перейти на **Starter Plan ($7/мес)**:

- ✅ Не засыпает
- ✅ Больше ресурсов
- ✅ Приоритетная поддержка

Upgrade: **Settings** → **Plan** → **Upgrade to Starter**

---

## 🐛 Troubleshooting

### Проблема: Деплой Failed

**Решение:**
1. Откройте вкладку **"Logs"**
2. Найдите ошибку (обычно внизу логов)
3. Проверьте `requirements.txt` и код

### Проблема: Сервис не запускается

**Решение:**
1. Проверьте переменную окружения `WB_API_KEY`
2. Убедитесь, что `Start Command` = `python main.py`
3. Проверьте логи на ошибки импорта модулей

### Проблема: 404 Not Found

**Решение:**
- Подождите полного завершения деплоя
- Проверьте URL (должен быть без `/static` и других путей)

---

## 📞 Нужна помощь?

Если что-то не получается на любом этапе — напишите мне:
- На каком шаге застряли
- Скриншот ошибки
- Текст из логов Render

Помогу разобраться! 🚀
