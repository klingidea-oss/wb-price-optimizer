# 🚀 Публикация дашборда через ngrok (2 минуты)

## Что это даст?

Ваше приложение станет доступно по красивой ссылке типа:
```
https://wb-optimizer-abc123.ngrok-free.app
```

Без упоминания "genspark" или других сервисов!

## 📋 Шаг 1: Установите ngrok (1 минута)

### Windows:
```bash
# Скачайте с официального сайта
https://ngrok.com/download

# Или через Chocolatey
choco install ngrok
```

### macOS:
```bash
brew install ngrok
```

### Linux:
```bash
# Ubuntu/Debian
sudo snap install ngrok

# Или скачайте напрямую
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar xvzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/
```

## 📋 Шаг 2: Зарегистрируйтесь (30 секунд)

1. Перейдите на https://ngrok.com/
2. Нажмите **"Sign up"** (можно через Google/GitHub)
3. Скопируйте ваш **authtoken**

## 📋 Шаг 3: Настройте ngrok (10 секунд)

```bash
ngrok config add-authtoken ВАШ_ТОКЕН
```

## 📋 Шаг 4: Запустите приложение (2 команды!)

### Терминал 1 - Запустите приложение:
```bash
cd wb_price_optimizer
python main.py
```

### Терминал 2 - Опубликуйте в интернет:
```bash
ngrok http 8000
```

## 🎉 Готово!

Вы увидите:
```
ngrok

Session Status                online
Account                       ваш_email (Plan: Free)
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**Ваша ссылка:** `https://abc123.ngrok-free.app`

Откройте её в браузере - ваш дашборд доступен всему миру! 🌍

## 💡 Советы

### Красивый домен (платно, но дешево)

Бесплатная версия дает случайный URL. Для постоянного красивого домена:

**ngrok Pro ($8/месяц):**
```bash
ngrok http 8000 --domain=wb-optimizer.ngrok.app
```

Получите постоянный адрес: `https://wb-optimizer.ngrok.app`

### Держать запущенным 24/7

Для постоянной работы используйте:

**Linux/Mac:**
```bash
# Создайте скрипт
nano start_dashboard.sh

# Добавьте:
#!/bin/bash
cd /path/to/wb_price_optimizer
python main.py &
sleep 5
ngrok http 8000

# Сделайте исполняемым
chmod +x start_dashboard.sh

# Запустите
./start_dashboard.sh
```

**Windows (PowerShell):**
```powershell
# Создайте start_dashboard.ps1
cd C:\path\to\wb_price_optimizer
Start-Process python -ArgumentList "main.py"
Start-Sleep -Seconds 5
ngrok http 8000
```

## 🔒 Безопасность

### Добавьте пароль (опционально)

ngrok поддерживает базовую аутентификацию:

```bash
ngrok http 8000 --basic-auth="username:password"
```

Теперь для доступа нужен логин/пароль!

### Только для определенных IP (ngrok Pro)

```bash
ngrok http 8000 --cidr-allow="123.45.67.89/32"
```

## 🎨 Кастомизация

### Изменить название в ссылке (ngrok Pro)

```bash
ngrok http 8000 --domain=my-wb-dashboard.ngrok.app
```

### Статический домен (ngrok Pro)

Получите один раз домен и используйте всегда:
```
https://your-company-dashboard.ngrok.app
```

## 📊 Мониторинг

ngrok предоставляет веб-интерфейс для мониторинга:
```
http://localhost:4040
```

Здесь видно:
- Все HTTP запросы
- Время ответа
- Статус коды
- Подробности каждого запроса

## ⚠️ Ограничения бесплатной версии

- ✅ Неограниченный трафик
- ✅ HTTPS автоматически
- ❌ URL меняется при перезапуске
- ❌ Только 1 одновременный туннель
- ❌ Сессия живёт 2 часа (потом переподключение)

### Решение ограничений:

**Для постоянного URL:** ngrok Pro ($8/мес)  
**Или используйте бесплатные облачные платформы** (см. ниже)

## 🚀 Альтернатива: Бесплатные облачные платформы

Если нужен постоянный URL **бесплатно**, используйте:

### Railway.app (Рекомендую!)
- Постоянный домен: `your-app.railway.app`
- Всегда онлайн
- Автоматические обновления
- HTTPS бесплатно

Я подготовил файлы для Railway - см. `DEPLOY_RAILWAY.md`

### Render.com
- Домен: `your-app.onrender.com`
- 500 часов/месяц бесплатно

### Fly.io
- Домен: `your-app.fly.dev`
- Постоянно работает

## 🎯 Рекомендации

**Для тестирования (сейчас):**
→ Используйте **ngrok** - запускается за 2 минуты!

**Для продакшена (постоянно):**
→ Используйте **Railway.app** - бесплатный постоянный домен

**Для корпоративного использования:**
→ Используйте **ваш сервер** + Docker (см. `DEPLOY_DOCKER.md`)

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте, что приложение работает локально: `http://localhost:8000`
2. Проверьте, что ngrok запущен: должны видеть "Forwarding" URL
3. Откройте ngrok web interface: `http://localhost:4040`
4. Проверьте firewall - может блокировать ngrok

## 🎉 Готово!

Теперь ваш дашборд доступен по красивой ссылке!

**Поделитесь ссылкой с коллегами** - они смогут открыть дашборд и работать с ним! 🚀

---

💡 **Совет:** Добавьте ссылку в закладки и запускайте скрипт при необходимости!
